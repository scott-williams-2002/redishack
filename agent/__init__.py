"""Agent package initializer.

This makes the `agent` folder a proper Python package so development helpers
and tools can be organized under `agent.tools` and imported from
`agent.agent` using package-relative imports.
"""

__all__ = ["agent"]
